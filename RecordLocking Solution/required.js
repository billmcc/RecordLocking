var loginListener = {};loginListener = function (username, password) {		var request, response;		//this will make it use the directory...	if(username == "Admin") {		return false;	}	request = {};	request.username = username;	request.password = password;		response = ds.Locking.REST_Login(request);		if (response.login === true) {		return {			name: username,			fullName: username,			storage:{				test: response.storage.test			}		};	}	else {		return {			error: 100, 			errorMessage: "Login failed."		};	}};